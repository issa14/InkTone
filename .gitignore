# Build outputs
build/
.gradle/
.kotlin/
*/build/
out/

# Local configuration (machine-specific, never committed)
local.properties
keystore.properties
*.jks
*.jks.bak
*.keystore

# Firebase / Google Services (secrets, added per-environment)
google-services.json

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store

# Legacy leftovers not (yet) part of the rewritten module tree
# (voice model assets, prebuilt native libs, generated config) —
# reintroduced deliberately module by module, never swept in by a
# bare `git add .`.
app/libs/
app/src/main/assets/models/
app/src/main/java/
app/google-services.json
benchmark/src/
