package com.inktone.domain.model

/** Thème système de l'application (chrome, barres, boutons) —
 * distinct de [ReadingTheme] qui régit uniquement la surface de lecture. */
enum class AppTheme { SYSTEM, LIGHT, DARK }
